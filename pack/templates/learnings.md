# Learnings

This repo's memory — a single lean file. Kernel guardrails live in
`skills/closed-loop/gates.md`; this repo's stack/git/trust facts in `context/`.
Keep it short: Standing rules are durable, Notes are recent one-liners.
Prune aggressively — a ledger nobody reads is dead weight.

## Standing rules (always apply)

## Notes (recent — newest first)
