# Release

**When:** shipping, deploying, or closing a whole-app run.

`agents/release.md` → `agents/monitor.md` → `agents/curator.md`.

Ops detail (only if the host documents it): `references/INDEX.md` → `docs/deploy.md` / `docs/runbook.md`. Git policy: `context/git.md`.
