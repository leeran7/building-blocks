# Debug

**When:** test or CI failure with an unclear cause.

Read `agents/debugger.md`. Use `context/gates.json` commands. Do not start at `archive/` or `loop/learnings.jsonl`.

If the failure is clearly a product bug, go to `workflows/implement.md` instead.
