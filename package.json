{
  "name": "building-blocks",
  "private": true,
  "scripts": {
    "loop": "pnpm --dir orchestrator loop",
    "sync": "node scripts/sync.mjs",
    "test:orchestrator": "yarn --cwd orchestrator test"
  }
}
