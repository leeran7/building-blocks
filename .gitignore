# Loop runtime artifacts
.cursor/loop/handoffs/*.json
.cursor/loop/state.json
.cursor/loop/*.md
# loop/* with negation to keep learnings.md (open questions only).
# Handoff JSONs are tracked so the orchestrator retro can promote findings.
loop/*
!loop/learnings.md
!loop/handoffs/
!loop/*.md
.claude/handoffs/*.json

# Dependencies & build
node_modules/
dist/
*.log

# Environment
.env
.env.local

# Deploy tooling (may contain auth tokens / local project links)
.vercel
.firebaserc
scratch
.next