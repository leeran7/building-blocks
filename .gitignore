# Loop runtime artifacts
.cursor/loop/handoffs/*.json
.cursor/loop/state.json
.cursor/loop/*.md
# Excludes loop/state.json, loop/handoffs/ and the per-run stage outputs, but NOT
# the learnings ledger: INDEX.md / RULES.md define that as persistent memory
# "never deleted between runs", and ignoring it made it vanish on every clone.
# Must be `loop/*` rather than `loop/`, or the negations below cannot re-include.
loop/*
!loop/learnings.md
!loop/learnings.jsonl
!loop/INDEX.md
# Spec, architecture, cost, schema-target, and handoffs stay local. Tracking
# them made every agent-notes commit a Vercel production deploy. Do not
# add `!loop/spec.md` (or architecture/handoffs) back.
# package-upgrade moved to archive/package-upgrade.md (progressive-disclosure
# filesystem). Handoffs are per-run scratch (loop/INDEX.md).
.claude/handoffs/*.json

# Dependencies & build
node_modules/
dist/
*.log

# Environment
.env
.env.local
